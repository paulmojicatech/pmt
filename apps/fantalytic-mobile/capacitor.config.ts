import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.fantalytica',
  appName: 'Fantalytic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
